import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // All images are now local — no remote patterns needed.
  // Add entries here if you ever use an external image CDN.
}

export default nextConfig
